# 붉은 벽돌 기록관 Google Sheets 연결

게임은 Apps Script 주소가 비어 있거나 연결에 실패하면 해당 기기의
`localStorage`에 기록을 보관합니다. 아래 설정을 마치면 참가자의 답장이
Google 시트에 저장되고 모든 참가자의 기록관에 바로 표시됩니다.

## 1. 비공개 Google 시트 만들기

1. 새 Google 시트를 만듭니다. 시트를 웹에 게시하거나 링크 공개하지 마세요.
2. 시트 주소의 `/d/`와 `/edit` 사이 문자열이 시트 ID입니다.
3. **확장 프로그램 > Apps Script**를 엽니다.
4. 기본 코드를 지우고 `google-apps-script.gs` 전체를 붙여 넣습니다.
5. 코드 상단의 `SPREADSHEET_ID`에 시트 ID를 입력하고 저장합니다.

## 2. Apps Script 웹 앱 배포

1. Apps Script 오른쪽 위 **배포 > 새 배포**를 누릅니다.
2. 유형은 **웹 앱**, 실행 사용자는 **나**, 액세스 권한은 **모든 사용자**로 지정합니다.
3. 배포하고 권한 요청을 승인한 뒤 `/exec`로 끝나는 웹 앱 URL을 복사합니다.
4. `database-config.js`의 `scriptUrl`에 URL을 붙여 넣습니다.

코드를 수정한 뒤에는 **배포 > 배포 관리 > 새 버전**으로 다시 배포해야 변경이
실제 게임에 반영됩니다.

## 3. 기록 관리

새 답장은 저장 즉시 공개됩니다. 게임은 15초마다 기록을 다시 확인하므로
다른 참가자의 답장도 새로고침 없이 붉은 벽돌 기록관에 나타납니다.
공개하지 않을 기록은 Google 시트에서 해당 행을 삭제하면 됩니다.

## 4. GitHub Pages에서 확인

변경 파일을 GitHub의 `main` 브랜치에 올린 뒤 아래 주소에서 확인합니다.

`https://seungmin999.github.io/mokpo_red_bricks/`

Apps Script URL은 브라우저에 보이는 공개 API 주소이지만 Google 시트 자체를
공개할 필요는 없습니다. 연락처와 URL 형태의 입력은 브라우저와 Apps Script에서
모두 거부하며, 같은 기기의 연속 등록은 60초 동안 제한됩니다.
